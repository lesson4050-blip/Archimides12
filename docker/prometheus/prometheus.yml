global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: archimedes-backend
    bearer_token: "${METRICS_BEARER_TOKEN}"
    static_configs:
      - targets: ['archimedes-backend:8000']
    metrics_path: /metrics
